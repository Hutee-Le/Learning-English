<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ttdaa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TTDAA" >
        <activity
            android:name=".login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".choingay_next"/>
        <activity android:name=".huongdan"/>
        <activity android:name=".Quenstion_de"/>
        <activity android:name=".activity_result"/>
        <activity android:name=".Question_kho"/>
        <activity android:name=".ChooseNumber"/>
        <activity android:name=".activity_highscore"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".Register"/>
    </application>

</manifest>